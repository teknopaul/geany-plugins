geanyagent plugin fixes

When we run commands into the Agent tab via agenttools.conf, as well as copy pasting the text, we should make the Agent tab have focus so that just Enter submits the AI instruction,.

When we run "Execute prompt file" we should save the file first, so the agent gets the latest version.
